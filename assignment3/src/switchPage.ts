const switchPage = (switchTo: HTMLElement, switchFrom: HTMLElement): void => {
  // const messageList = (document.getElementById("messages").style.visibility =
  //   "visible");
  switchTo.hidden = false;
  switchFrom.hidden = true;
  // const form = (document.getElementById("form").style.visibility = "hidden");
};

export default switchPage;
